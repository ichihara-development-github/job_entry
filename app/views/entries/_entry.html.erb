          <% if current_user.admin %>
          　<%= render "shared/actions", table: "jobs", obj: obj %>
          <% elsif Entry.find_by(user_id: current_user.id, job_id: obj.id) %>
            <button class = "btn btn-primary" disabled>エントリー済み</button>
          <% else %>
            <%= link_to "エントリー", job_entry_path(obj), method: :post , class: "btn-primary btn" %>
          <% end %>
